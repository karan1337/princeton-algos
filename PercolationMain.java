/**
 * Created by Karan on 5/22/16.
 */

import edu.princeton.cs.algs4.StdIn;
import edu.princeton.cs.algs4.StdOut;

public class PercolationMain {
    public static void main(String args[]){

        /*
        Percolation per = new Percolation(10);
        per.open(1,1);
        per.open(1,2);
        StdOut.println(per.wqf.connected(per.xyTo1D(1,1), per.xyTo1D(1,2)));
        */
    /*
        int N = StdIn.readInt();
        Percolation per = new Percolation(N);

        while (!StdIn.isEmpty()) {
            int p = StdIn.readInt();
            int q = StdIn.readInt();
            if (per.wqf.connected(p, q)) continue;

            per.wqf.union(p, q);
            //per.wqf.printIDs();
            StdOut.println(p + " " + q);
        }
        StdOut.println(per.wqf.count() + " components");
    */

    }

}
